import React from 'react';
import { storiesOf } from '@storybook/react';
import { LiveLabel } from '../..';

storiesOf('Elements|LiveLabel', module).add('default', () => <LiveLabel>● LIVE</LiveLabel>);
