import React from 'react';
import { Card, CardHeader, CardContent, CardFooter } from './base/index';

const ArticleCard = () => (
  <Card>
    <CardHeader />
    <CardContent />
    <CardFooter />
  </Card>
);

ArticleCard.displayName = 'ArticleCard';

export default ArticleCard;
